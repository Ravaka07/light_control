import 'package:flutter/material.dart';
import 'base_sidebar.dart';

class Control extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BasePage(
      title: 'Control',
      child: Center(child: Text('Bienvenue sur la page control!')),
    );
  }
}
